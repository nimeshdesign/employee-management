// Shared between Employees.jsx (table inline-edit cells) and
// EmployeeCard.jsx (grid inline-edit cards) — same visual treatment, so
// one definition instead of two copies drifting apart over time.
export const INLINE_INPUT_CLASS =
  'w-full rounded-md border border-gray-300 px-2 py-1 text-sm outline-none focus:ring-2 focus:ring-primary/50 dark:border-gray-600 dark:bg-gray-900 dark:text-gray-100'
